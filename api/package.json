{
  "name": "portfolio-api",
  "version": "1.0.0",
  "description": "Azure Functions API for Jamie Pryce Portfolio",
  "main": "index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "func start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.3"
  }
}
